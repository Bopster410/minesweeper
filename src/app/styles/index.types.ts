export type Variables = {
    backgroundColor: string;
    fontColor: string;
    fontColorSecondary: string;
    borderColor: string;
    hoverColor: string;
};

export type Theme = 'light' | 'dark';
